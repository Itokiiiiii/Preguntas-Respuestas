version: "3.8"

services:
  mongo:
    image: mongo:7
    container_name: mongo_trivia
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: trivia
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: